import React from "react";
import {Content} from "../components";
import PageLayout from "../common/PageLayout";

const Consumers = () => {
  return (
    <PageLayout>
      <Content pageType='consumer' />
    </PageLayout>
  );
};

export default Consumers;
